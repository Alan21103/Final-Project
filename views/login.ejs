<video autoplay muted loop>
  <source src="/image/71122-537102350_small.mp4" type="video/mp4">
  Your browser does not support the video tag.
</video>

<div class="login-container">
  <div class="login-card">
    <div class="avatar">
      <img src="/image/download.png" alt="Avatar Image">
    </div>
    <h2>Login</h2>
    <h3>Welcome to Orange Hotel!</h3>
    <form action="/login" method="POST" class="login-form">
      <!-- Username Input -->
      <div class="input-group mb-3">
        <span class="input-group-text"><i class="bi bi-person"></i></span>
        <input type="text" class="form-control" placeholder="Username" name="username" required>
      </div>
      <!-- Password Input -->
      <div class="input-group mb-3">
        <span class="input-group-text"><i class="bi bi-lock"></i></span>
        <input type="password" class="form-control" placeholder="Password" name="password" required>
      </div>
      <!-- Login Button -->
      <button type="submit" class="btn-login">Login</button>
      <p class="mt-3">Don't have an account? <a href="/signup">Sign up here</a>.</p>
    </form>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
      const currentPath = window.location.pathname;

      // Sembunyikan navbar pada halaman login dan signup
      if (currentPath === '/login' || currentPath === '/signup') {
          document.querySelector('.navbar').style.display = 'none';
      }
  });
</script>